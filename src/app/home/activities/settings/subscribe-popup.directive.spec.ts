import { SubscribePopupDirective } from './subscribe-popup.directive';

describe('SubscribePopupDirective', () => {
  it('should create an instance', () => {
    const directive = new SubscribePopupDirective();
    expect(directive).toBeTruthy();
  });
});
