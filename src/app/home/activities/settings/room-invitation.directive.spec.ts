import { RoomInvitationDirective } from './room-invitation.directive';

describe('RoomInvitationDirective', () => {
  it('should create an instance', () => {
    const directive = new RoomInvitationDirective();
    expect(directive).toBeTruthy();
  });
});
