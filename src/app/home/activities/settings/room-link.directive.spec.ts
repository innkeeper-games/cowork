import { RoomLinkDirective } from './room-link.directive';

describe('RoomLinkDirective', () => {
  it('should create an instance', () => {
    const directive = new RoomLinkDirective();
    expect(directive).toBeTruthy();
  });
});
