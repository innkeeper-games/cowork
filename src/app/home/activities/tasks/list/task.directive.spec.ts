import { TaskDirective } from './task.directive';

describe('TaskDirective', () => {
  it('should create an instance', () => {
    const directive = new TaskDirective();
    expect(directive).toBeTruthy();
  });
});
