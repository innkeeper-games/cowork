import { FilterPopupDirective } from './filter-popup.directive';

describe('FilterPopupDirective', () => {
  it('should create an instance', () => {
    const directive = new FilterPopupDirective();
    expect(directive).toBeTruthy();
  });
});
