import { TagsPopupDirective } from './tags-popup.directive';

describe('TagsPopupDirective', () => {
  it('should create an instance', () => {
    const directive = new TagsPopupDirective();
    expect(directive).toBeTruthy();
  });
});
