import { TaskEditorPopupDirective } from './task-editor-popup.directive';

describe('TaskEditorPopupDirective', () => {
  it('should create an instance', () => {
    const directive = new TaskEditorPopupDirective();
    expect(directive).toBeTruthy();
  });
});
