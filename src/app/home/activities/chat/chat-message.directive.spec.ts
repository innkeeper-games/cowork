import { ChatMessageDirective } from './chat-message.directive';

describe('ChatMessageDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new ChatMessageDirective();
    expect(directive).toBeTruthy();
  });
});
