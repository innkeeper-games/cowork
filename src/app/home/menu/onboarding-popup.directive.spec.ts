import { OnboardingPopupDirective } from './onboarding-popup.directive';

describe('OnboardingPopupDirective', () => {
  it('should create an instance', () => {
    const directive = new OnboardingPopupDirective();
    expect(directive).toBeTruthy();
  });
});
