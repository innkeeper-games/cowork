import { VendorPopupDirective } from './vendor-popup.directive';

describe('VendorPopupDirective', () => {
  it('should create an instance', () => {
    const directive = new VendorPopupDirective();
    expect(directive).toBeTruthy();
  });
});
