import { InventoryPopupDirective } from './inventory-popup.directive';

describe('InventoryPopupDirective', () => {
  it('should create an instance', () => {
    const directive = new InventoryPopupDirective();
    expect(directive).toBeTruthy();
  });
});
