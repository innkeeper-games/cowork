import { EditItemTooltipDirective } from './edit-item-tooltip.directive';

describe('EditItemTooltipDirective', () => {
  it('should create an instance', () => {
    const directive = new EditItemTooltipDirective();
    expect(directive).toBeTruthy();
  });
});
