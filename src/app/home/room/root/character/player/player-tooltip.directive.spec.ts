import { PlayerTooltipDirective } from './player-tooltip.directive';

describe('PlayerTooltipDirective', () => {
  it('should create an instance', () => {
    const directive = new PlayerTooltipDirective();
    expect(directive).toBeTruthy();
  });
});
